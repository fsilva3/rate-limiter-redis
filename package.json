{
  "name": "tb-redis",
  "version": "0.0.1",
  "description": "A javascript library to handle Token Bucket algorithm for Redis Server where you can sync the tokens between multiple services instances",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Filipe Silva",
  "license": "MIT",
  "dependencies": {
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@types/node": "^22.13.4",
    "eslint": "^9.20.1",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.24.0",
    "vitest": "^3.0.5"
  }
}
